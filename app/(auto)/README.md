# app/(auto)/

> L2 | Parent: [app/README.md](../README.md)

## Members

- `layout.tsx`: Wraps x-default pages in an English document and runs locale resolution before paint.
- `page.tsx`: Renders the English home fallback while publishing Auto metadata.
- `changelog/`: Renders the English Changelog fallback.
- `features/`: Renders four English feature fallbacks.

The URL remains the Auto entry. JavaScript chooses a canonical locale-prefixed URL; without JavaScript, the complete English page remains readable.

[PROTOCOL]: Update this file when members or responsibilities change, then verify the parent README.md.
