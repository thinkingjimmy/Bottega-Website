# app/[locale]/features/[slug]/

> L2 | Parent: [app/[locale]/features/README.md](../README.md)

- `page.tsx`: Resolves locale and slug, publishes descriptive search metadata from that feature's localized catalog entry, and renders the shared feature view.

[PROTOCOL]: Update this header when making changes, then check README.md.
