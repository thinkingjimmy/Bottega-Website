# app/(english)/

> L2 | Parent: [app/README.md](../README.md)

## Members

- `layout.tsx`: Wraps every unprefixed page in the canonical English document.
- `page.tsx`: Renders the canonical English home page.
- `changelog/`: Renders the canonical English Changelog page.
- `features/`: Renders four canonical English feature pages.

English and `x-default` share each unprefixed URL. Language changes happen only through explicit links, so the URL remains the sole content-language authority.

[PROTOCOL]: Update this file when members or responsibilities change, then verify the parent README.md.
